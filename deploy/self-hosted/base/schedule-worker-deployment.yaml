apiVersion: apps/v1
kind: Deployment
metadata:
  name: iolaus-schedule-worker
  labels:
    app.kubernetes.io/name: iolaus
    app.kubernetes.io/component: schedule-worker
    app.kubernetes.io/part-of: iolaus
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: iolaus
      app.kubernetes.io/component: schedule-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: iolaus
        app.kubernetes.io/component: schedule-worker
        app.kubernetes.io/part-of: iolaus
    spec:
      terminationGracePeriodSeconds: 90
      securityContext:
        fsGroup: 10001
        runAsGroup: 10001
        runAsNonRoot: true
        runAsUser: 10001
        seccompProfile:
          type: RuntimeDefault
      imagePullSecrets:
        - name: iolaus-registry
      automountServiceAccountToken: false
      initContainers:
        - name: migrate
          image: ghcr.io/willgriffin/iolaus/site@sha256:REPLACE_WITH_RELEASED_IMAGE_DIGEST
          command:
            - /bin/sh
            - -ec
            - cd /app && node --import tsx apps/site/scripts/db-migrate.ts
          envFrom:
            - secretRef:
                name: iolaus-migration-runtime
          env:
            - name: SMRT_RUNTIME_PROFILE
              value: self-hosted
            - name: HOME
              value: /tmp
            - name: XDG_CACHE_HOME
              value: /tmp/.cache
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: runtime-tmp
              mountPath: /tmp
      containers:
        # Only this deployment creates due schedules. Provider crawls remain
        # task jobs, so the task worker is independently drainable and visible.
        - name: schedule-worker
          image: ghcr.io/willgriffin/iolaus/site@sha256:REPLACE_WITH_RELEASED_IMAGE_DIGEST
          command: ["node", "scripts/smrt-worker.mjs", "schedule"]
          envFrom:
            - secretRef:
                name: iolaus-runtime
          env:
            - name: NODE_ENV
              value: production
            - name: SMRT_RUNTIME_PROFILE
              value: self-hosted
            - name: SMRT_WORKER_HEARTBEAT_FILE
              value: /var/run/iolaus/worker-heartbeat.json
            - name: SMRT_WORKER_HEARTBEAT_MAX_AGE_MS
              value: "45000"
            - name: HOME
              value: /tmp
            - name: XDG_CACHE_HOME
              value: /tmp/.cache
          volumeMounts:
            - name: worker-health
              mountPath: /var/run/iolaus
            - name: runtime-tmp
              mountPath: /tmp
          startupProbe:
            exec:
              command: ["node", "scripts/smrt-worker-heartbeat.mjs", "--check"]
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 24
          livenessProbe:
            exec:
              command: ["node", "scripts/smrt-worker-heartbeat.mjs", "--check"]
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
      volumes:
        - name: worker-health
          emptyDir: {}
        - name: runtime-tmp
          emptyDir: {}
