apiVersion: apps/v1
kind: Deployment
metadata:
  name: iolaus-task-worker
  labels:
    app.kubernetes.io/name: iolaus
    app.kubernetes.io/component: task-worker
    app.kubernetes.io/part-of: iolaus
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: iolaus
      app.kubernetes.io/component: task-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: iolaus
        app.kubernetes.io/component: task-worker
        app.kubernetes.io/part-of: iolaus
    spec:
      # The runner drains work for up to 60 seconds after SIGTERM; leave margin
      # for provider cancellation and the process shutdown sequence.
      terminationGracePeriodSeconds: 90
      securityContext:
        fsGroup: 10001
        runAsGroup: 10001
        runAsNonRoot: true
        runAsUser: 10001
        seccompProfile:
          type: RuntimeDefault
      imagePullSecrets:
        - name: iolaus-registry
      initContainers:
        - name: migrate
          image: ghcr.io/willgriffin/iolaus/site@sha256:REPLACE_WITH_RELEASED_IMAGE_DIGEST
          command:
            - /bin/sh
            - -ec
            - cd /app && pnpm --filter @willgriffin/iolaus-site db:migrate
          envFrom:
            - secretRef:
                name: iolaus-migration-runtime
          env:
            - name: SMRT_RUNTIME_PROFILE
              value: self-hosted
            - name: HOME
              value: /tmp
            - name: XDG_CACHE_HOME
              value: /tmp/.cache
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: runtime-tmp
              mountPath: /tmp
      containers:
        - name: task-worker
          image: ghcr.io/willgriffin/iolaus/site@sha256:REPLACE_WITH_RELEASED_IMAGE_DIGEST
          command: ["pnpm", "worker"]
          envFrom:
            - secretRef:
                name: iolaus-runtime
          env:
            - name: NODE_ENV
              value: production
            - name: SMRT_RUNTIME_PROFILE
              value: self-hosted
            - name: SMRT_WORKER_CONCURRENCY
              value: "4"
            - name: SMRT_WORKER_HEARTBEAT_FILE
              value: /var/run/iolaus/worker-heartbeat.json
            - name: SMRT_WORKER_HEARTBEAT_MAX_AGE_MS
              value: "45000"
            - name: HOME
              value: /tmp
            - name: XDG_CACHE_HOME
              value: /tmp/.cache
          volumeMounts:
            - name: worker-health
              mountPath: /var/run/iolaus
            - name: runtime-tmp
              mountPath: /tmp
          startupProbe:
            exec:
              command: ["node", "scripts/smrt-worker-heartbeat.mjs", "--check"]
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 24
          livenessProbe:
            exec:
              command: ["node", "scripts/smrt-worker-heartbeat.mjs", "--check"]
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
      volumes:
        - name: worker-health
          emptyDir: {}
        - name: runtime-tmp
          emptyDir: {}
