apiVersion: apps/v1
kind: Deployment
metadata:
  name: iolaus-web
  labels:
    app.kubernetes.io/name: iolaus
    app.kubernetes.io/component: web
    app.kubernetes.io/part-of: iolaus
spec:
  replicas: 2
  minReadySeconds: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: iolaus
      app.kubernetes.io/component: web
  template:
    metadata:
      labels:
        app.kubernetes.io/name: iolaus
        app.kubernetes.io/component: web
        app.kubernetes.io/part-of: iolaus
    spec:
      # The migration command is serialized by PostgreSQL's advisory lock. Each
      # independently rolling workload still runs it before its own process.
      terminationGracePeriodSeconds: 90
      securityContext:
        fsGroup: 10001
        runAsGroup: 10001
        runAsNonRoot: true
        runAsUser: 10001
        seccompProfile:
          type: RuntimeDefault
      imagePullSecrets:
        - name: iolaus-registry
      automountServiceAccountToken: false
      initContainers:
        - name: migrate
          image: ghcr.io/willgriffin/iolaus/site@sha256:REPLACE_WITH_RELEASED_IMAGE_DIGEST
          command:
            - /bin/sh
            - -ec
            - cd /app && node --import tsx apps/site/scripts/db-migrate.ts
          envFrom:
            - secretRef:
                name: iolaus-migration-runtime
          env:
            - name: SMRT_RUNTIME_PROFILE
              value: self-hosted
            - name: HOME
              value: /tmp
            - name: XDG_CACHE_HOME
              value: /tmp/.cache
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: runtime-tmp
              mountPath: /tmp
      containers:
        - name: web
          image: ghcr.io/willgriffin/iolaus/site@sha256:REPLACE_WITH_RELEASED_IMAGE_DIGEST
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          envFrom:
            - secretRef:
                name: iolaus-runtime
          env:
            - name: NODE_ENV
              value: production
            - name: SMRT_RUNTIME_PROFILE
              value: self-hosted
            - name: HOST
              value: 0.0.0.0
            - name: PORT
              value: "3000"
            - name: HOME
              value: /tmp
            - name: XDG_CACHE_HOME
              value: /tmp/.cache
          lifecycle:
            preStop:
              exec:
                # Give EndpointSlice propagation time before Node receives SIGTERM.
                command: ["/bin/sh", "-ec", "sleep 10"]
          startupProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 24
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /live
              port: http
            periodSeconds: 30
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "1"
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: runtime-tmp
              mountPath: /tmp
      volumes:
        # Node and Chromium only receive this disposable writable surface;
        # application assets remain in the configured external object store.
        - name: runtime-tmp
          emptyDir: {}
