# @willgriffin/iolaus-cli Agent Guide

Follow the repository-level `AGENTS.md` first. This package contains the
authenticated command-line and MCP bridge for iolaus.localhost.

Keep generated `dist/` output out of source changes unless the package publish
workflow explicitly requires it.
